import { IBucket, IUpload } from ".";

const UserBucket: IBucket = {
    name: "user",
    region: ""
}

/*
export interface UserUpload {
    fileName: string;
    //file: ReadStream
    size: number;
    buffer: Buffer;
}
*/