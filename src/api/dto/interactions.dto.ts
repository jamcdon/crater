//sql

export type ComposeInteractionDTO = {
    composeID?: string,
    imageID?: string,
    UserId: number,
    commentID?: string,
    comment: boolean,
    star: boolean,
    creator: boolean
}