apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: crater-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/crater-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/crater-net: "true"
